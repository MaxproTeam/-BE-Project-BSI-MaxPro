import {getUsers, getUser} from '../models/UserModel.js';

const authServices = {
    loginUser: async (params) => {
        
    },
  
    registerUser: async (userData) => {
      
    },
};

export default authServices